# out/ holds per-run diff artifacts (impl/figma png, findings, report.html, crops/).
# Regenerable via run.mjs; live URL is the canonical share path
# (https://spxpay-tech.github.io/figma-fidelity-reports/sgb-to-spx/report.html).
out/

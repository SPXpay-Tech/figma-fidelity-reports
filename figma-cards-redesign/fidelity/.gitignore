# storage.json is a Playwright storageState minted by make-storage-state.mjs — it embeds a staging
# session + the login response user blob (bcrypt hash + PII). Regenerable; never commit.
storage.json
# out/ holds per-run diff artifacts (impl/figma png, findings, report.html). Regenerable via run.mjs.
out/
